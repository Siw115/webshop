import { Product } from "./product.model";

export class OrderLine {
    variantId: number;
    quantity: number;
    productName: string;
    productDescription: string;
    productPrice: number;
    variantColor: string;
    variantSize: string;

}
